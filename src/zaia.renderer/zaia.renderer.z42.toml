[project]
name        = "zaia.renderer"
kind        = "lib"
version     = "0.1.0"
license     = "MIT"
description = "Render: the RenderBackend seam, the reconciler, and the HTML (server-side) backend."

[dependencies]
"zaia.ui" = "0.1.0"

[sources]
include = ["src/**/*.z42"]
