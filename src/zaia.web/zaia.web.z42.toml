[project]
name        = "zaia.web"
kind        = "lib"
version     = "0.1.0"
license     = "MIT"
description = "The browser host: DomBackend over the VM's __dom_* builtins. Runs under wasm32 only."

[dependencies]
"zaia.renderer" = "0.1.0"

[sources]
include = ["src/**/*.z42"]
