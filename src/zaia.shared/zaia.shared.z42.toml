[project]
name        = "zaia.shared"
kind        = "lib"
version     = "0.1.0"
license     = "MIT"
description = "What both ends agree on: the page table routed identically on server and client, and typed API contracts."

[dependencies]
"zaia.core" = "0.1.0"
"zaia.ui"   = "0.1.0"

[sources]
include = ["src/**/*.z42"]
