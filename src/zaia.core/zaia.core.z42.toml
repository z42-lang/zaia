[project]
name        = "zaia.core"
kind        = "lib"
version     = "0.1.0"
license     = "MIT"
description = "Foundation seams shared by every zaia layer: results, route patterns, service container."

[sources]
include = ["src/**/*.z42"]
