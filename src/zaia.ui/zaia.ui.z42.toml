[project]
name        = "zaia.ui"
kind        = "lib"
version     = "0.1.0"
license     = "MIT"
description = "Describe: components, virtual nodes, element factory, and the change signal."

[sources]
include = ["src/**/*.z42"]
